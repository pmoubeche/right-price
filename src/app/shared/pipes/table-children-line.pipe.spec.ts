import { TableChildrenLinePipe } from './table-children-line.pipe';

describe('TableChildrenLinePipe', () => {
  it('create an instance', () => {
    const pipe = new TableChildrenLinePipe();
    expect(pipe).toBeTruthy();
  });
});
