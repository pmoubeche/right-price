import { UppercaseFirstLetterFormatPipe } from './uppercase-first-letter-format.pipe';

describe('UppercaseFirstLetterFormatPipe', () => {
  it('create an instance', () => {
    const pipe = new UppercaseFirstLetterFormatPipe();
    expect(pipe).toBeTruthy();
  });
});
