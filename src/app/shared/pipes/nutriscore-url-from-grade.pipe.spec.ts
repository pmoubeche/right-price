import { NutriscoreUrlFromGradePipe } from './nutriscore-url-from-grade.pipe';

describe('NutriscoreUrlFromGradePipe', () => {
  it('create an instance', () => {
    const pipe = new NutriscoreUrlFromGradePipe();
    expect(pipe).toBeTruthy();
  });
});
